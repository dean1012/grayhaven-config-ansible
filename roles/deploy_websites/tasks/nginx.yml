---
- name: Install nginx site configuration
  ansible.builtin.template:
    src: websites.conf.j2
    dest: /etc/nginx/conf.d/grayhaven-websites.conf
    owner: root
    group: root
    mode: "0644"
  notify: Reload nginx

- name: Remove legacy nginx static site configuration
  ansible.builtin.file:
    path: /etc/nginx/conf.d/grayhaven-static-sites.conf
    state: absent
  notify: Reload nginx

- name: Remove nginx default welcome configuration
  ansible.builtin.file:
    path: /etc/nginx/conf.d/default.conf
    state: absent
  notify: Reload nginx

- name: Install Certbot renewal deploy hook
  ansible.builtin.copy:
    dest: /etc/letsencrypt/renewal-hooks/deploy/reload-nginx.sh
    owner: root
    group: root
    mode: "0755"
    content: |
      #!/usr/bin/env bash
      set -Eeuo pipefail

      systemctl reload nginx
  when: deploy_websites_tls_mode == "host"

- name: Enable Certbot renewal timer when present
  ansible.builtin.systemd_service:
    name: certbot-renew.timer
    enabled: true
    state: started
  failed_when: false
  when: deploy_websites_tls_mode == "host"

- name: Enable and start nginx
  ansible.builtin.systemd_service:
    name: nginx
    enabled: true
    state: started
