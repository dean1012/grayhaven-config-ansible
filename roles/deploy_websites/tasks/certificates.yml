---
# Initial host certificates are issued only in host TLS mode. In load balancer
# TLS mode, OpenTofu owns the certificate attached to the DigitalOcean load
# balancer.
- name: Ensure Certbot environment marker directory exists
  ansible.builtin.file:
    path: /etc/grayhaven/certbot
    state: directory
    owner: root
    group: root
    mode: "0755"
  when: deploy_websites_tls_mode == "host"

- name: Check host TLS certificate files
  ansible.builtin.stat:
    path: "/etc/letsencrypt/live/{{ item.cert_name }}/fullchain.pem"
  loop: "{{ deploy_websites_certificate_sites }}"
  loop_control:
    label: "{{ item.cert_name }}"
  register: deploy_websites_certificate_file_stats
  when: deploy_websites_tls_mode == "host"

- name: Check Certbot environment markers
  ansible.builtin.stat:
    path: "/etc/grayhaven/certbot/{{ item.cert_name }}.environment"
  loop: "{{ deploy_websites_certificate_sites }}"
  loop_control:
    label: "{{ item.cert_name }}"
  register: deploy_websites_certificate_environment_marker_stats
  when: deploy_websites_tls_mode == "host"

- name: Read Certbot environment markers
  ansible.builtin.slurp:
    src: "/etc/grayhaven/certbot/{{ item.item.cert_name }}.environment"
  loop: >-
    {{
      deploy_websites_certificate_environment_marker_stats.results
      | default([])
      | selectattr('stat.exists')
      | list
    }}
  loop_control:
    label: "{{ item.item.cert_name }}"
  register: deploy_websites_certificate_environment_markers
  when: deploy_websites_tls_mode == "host"

- name: Build Certbot environment marker map
  ansible.builtin.set_fact:
    deploy_websites_certificate_environment_marker_map: >-
      {{
        deploy_websites_certificate_environment_marker_map | default({})
        | combine({
            item.item.item.cert_name: item.content | b64decode | trim
          })
      }}
  loop: "{{ deploy_websites_certificate_environment_markers.results | default([]) }}"
  loop_control:
    label: "{{ item.item.item.cert_name }}"
  when: deploy_websites_tls_mode == "host"

- name: Derive host TLS certificates requiring issuance
  ansible.builtin.set_fact:
    deploy_websites_certificates_requiring_issuance: >-
      {{
        deploy_websites_certificates_requiring_issuance | default([])
        + [item.item]
      }}
  loop: "{{ deploy_websites_certificate_file_stats.results }}"
  loop_control:
    label: "{{ item.item.cert_name }}"
  when:
    - deploy_websites_tls_mode == "host"
    - >-
      not item.stat.exists
      or (
        deploy_websites_certificate_environment_marker_map
        | default({})
      ).get(item.item.cert_name) != deploy_websites_certbot_environment

- name: Issue or renew Let's Encrypt certificates
  ansible.builtin.command:
    argv: "{{ deploy_websites_certbot_command }}"
  changed_when: true
  until: deploy_websites_certbot_issuance.rc == 0
  retries: 3
  delay: 20
  vars:
    deploy_websites_certbot_domains: "{{ [item.domain] + item.aliases + [item.dev_domain] }}"
    deploy_websites_certbot_domain_args: >-
      {{
        deploy_websites_certbot_domains
        | map('regex_replace', '^', '-d ')
        | join(' ')
        | split(' ')
      }}
    deploy_websites_certbot_command: >-
      {{
        [
          'certbot',
          'certonly',
          '--non-interactive',
          '--agree-tos',
          '--email', deploy_websites_certbot_email,
          '--cert-name', item.cert_name,
          '--force-renewal',
          '--dns-digitalocean',
          '--dns-digitalocean-credentials', deploy_websites_certbot_credentials_file,
          '--dns-digitalocean-propagation-seconds', '30'
        ]
        + (['--test-cert'] if deploy_websites_certbot_environment == 'staging' else [])
        + deploy_websites_certbot_domain_args
      }}
  loop: "{{ deploy_websites_certificates_requiring_issuance | default([]) }}"
  loop_control:
    label: "{{ item.cert_name }}"
  register: deploy_websites_certbot_issuance
  notify: Reload nginx
  when: deploy_websites_tls_mode == "host"

- name: Record Certbot certificate environment markers
  ansible.builtin.copy:
    dest: "/etc/grayhaven/certbot/{{ item.item.cert_name }}.environment"
    owner: root
    group: root
    mode: "0644"
    content: "{{ deploy_websites_certbot_environment }}\n"
  loop: "{{ deploy_websites_certbot_issuance.results | default([]) }}"
  loop_control:
    label: "{{ item.item.cert_name }}"
  when:
    - deploy_websites_tls_mode == "host"
    - item.rc == 0

- name: Verify host TLS certificate files exist
  ansible.builtin.stat:
    path: "/etc/letsencrypt/live/{{ item.cert_name }}/fullchain.pem"
  loop: "{{ deploy_websites_certificate_sites }}"
  loop_control:
    label: "{{ item.cert_name }}"
  register: deploy_websites_certificate_stats
  when: deploy_websites_tls_mode == "host"

- name: Fail if host TLS certificate files are missing
  ansible.builtin.assert:
    that:
      - deploy_websites_certificate_stats.results | map(attribute='stat.exists') | min
    fail_msg: "One or more host TLS certificate files are missing after Certbot execution."
    quiet: true
  when: deploy_websites_tls_mode == "host"
