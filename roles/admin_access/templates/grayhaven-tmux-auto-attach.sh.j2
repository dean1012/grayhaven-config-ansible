# Managed by Ansible.

if [[ $- == *i* ]] \
    && [ -t 0 ] \
    && [ -n "${SSH_CONNECTION:-}" ] \
    && [ -z "${TMUX:-}" ] \
    && [ -z "${GRAYHAVEN_TMUX_AUTO_ATTACH_BYPASS:-}" ]; then
    if command -v gtmux >/dev/null 2>&1; then
        gtmux
    fi
fi
