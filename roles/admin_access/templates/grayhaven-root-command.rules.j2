# Managed by Ansible.
# Root command review is handled by journald entries from
# /etc/profile.d/grayhaven-root-command-audit.sh and normal sudo logs.
#
# This file intentionally contains no execve audit rules. The earlier auditd
# rules were too noisy for operator review because each interactive root shell
# command generated events for child process execution.
