---
- name: Restart Alloy
  ansible.builtin.systemd_service:
    name: alloy.service
    state: restarted
    daemon_reload: true
  when: observability_metrics_enabled | default(false)

- name: Reload observability systemd
  ansible.builtin.systemd_service:
    daemon_reload: true
