---
- name: Restart Time Tracker
  ansible.builtin.systemd_service:
    name: "{{ timetracker_service_name }}"
    state: restarted
    daemon_reload: true

- name: Reload Time Tracker nginx configuration
  ansible.builtin.systemd_service:
    name: nginx
    state: reloaded
